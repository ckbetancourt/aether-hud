{
  "name": "aether-jarvis-hud",
  "private": true,
  "description": "Aether HUD — voice-first interface for Hermes Agent",
  "bin": {
    "aether": "bin/aether"
  },
  "scripts": {
    "start": "node scripts/free-port.js && node server.js",
    "hermes:doctor": "node scripts/hermes-doctor.js",
    "hermes:launch": "node scripts/aether-hermes-launch.js --open",
    "hermes:install-skill": "bash scripts/install-hermes-skill.sh",
    "test:attachment-preview": "node scripts/test-attachment-preview.mjs",
    "test:kanban-workspaces": "node scripts/test-kanban-workspaces.mjs"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
